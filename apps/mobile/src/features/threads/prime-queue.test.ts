import { describe, expect, it } from "vitest"; import { renderPrimeQueue, resolvePrimeSend } from "./primeQueue";
describe("prime queue", () => { it("requires explicit choice and capability", () => { expect(resolvePrimeSend(null,{steer:true},0).ok).toBe(false); expect(resolvePrimeSend("steer",{},0).ok).toBe(false); }); it("blocks attachments",()=>expect(resolvePrimeSend("steer",{steer:true},1).ok).toBe(false)); it("orders authoritative steering then follow ups without ids or cancel",()=>expect(renderPrimeQueue({steering:["a"],followUps:["b"]})).toEqual(["Steering: a","Queued: b"])); });