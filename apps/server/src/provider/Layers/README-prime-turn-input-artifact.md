# PA-M08 turn-input artifact

```sh
./generate-prime-turn-input-artifact.sh
cp prime-turn-input-artifact.mjs /tmp/ && cd /tmp
node prime-turn-input-artifact.mjs
```

The generated source-free artifact validates exact structured model identity, ambiguous legacy rejection, thinking configuration, deterministic text attachment inclusion, image base64/MIME payload, capability rejection, and command ordering.
